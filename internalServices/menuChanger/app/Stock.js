export default class Stock {
  constructor(idx, menuIdx, name, price, rest) {
    this.idx = idx;
    this.menuIdx = menuIdx;
    this.name = name;
    this.price = price;
    this.rest = rest;
  }
}
